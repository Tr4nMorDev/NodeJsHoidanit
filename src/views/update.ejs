<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css" />
    <title>Document</title>
  </head>
  <body>
    <!-- Thanh điều hướng -->
    <div class="nav3">
      <a href="/home">Trang Chủ</a>
      <a href="/login">Đăng nhập</a>
      <a href="/about">Giới Thiệu</a>
      <a href="/sample">Liên Hệ</a>
    </div>
    <!-- Ô nhập liệu -->
    <div class="input-container">
      <h2>Chỉnh sửa thông tin</h2>
      <form action="/updateUser/<%= usersEdit.customer_id %>" method="POST">
        <input
          type="text"
          placeholder="Nhập ten"
          name="name"
          value="<%= usersEdit.name %>"
        />
        <input
          type="email"
          placeholder="Nhập Email"
          name="email"
          value="<%= usersEdit.email %>"
        />
        <input
          type="text"
          placeholder="Nhập Số điện thoại"
          name="phone"
          value="<%= usersEdit.phone %>"
        />
        <input
          type="text"
          placeholder="Nhập địa chỉ"
          name="address"
          value="<%= usersEdit.address %>"
        />
        <button class="Cai-nut" type="submit">Save</button>
      </form>
    </div>
  </body>
</html>
